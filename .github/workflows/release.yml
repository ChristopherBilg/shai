# release.yml — create a CalVer GitHub Release on every push to main
name: Release

on:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate CalVer tag
        id: version
        run: |
          BASE="v$(date -u +%Y.%m.%d)"
          VERSION="$BASE"
          if git tag -l "$VERSION" | grep -q .; then
            N=1
            while git tag -l "${BASE}.${N}" | grep -q .; do
              N=$((N + 1))
            done
            VERSION="${BASE}.${N}"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build release tarball
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          STAGING="shai-${VERSION}"
          mkdir "$STAGING"
          git archive HEAD | tar x -C "$STAGING"
          echo "$VERSION" > "${STAGING}/VERSION"
          # REPO is what wf_suggest_repo files post-workflow suggestions on. An installed
          # tree has no .git, so it cannot derive this itself; taking it from
          # github.repository means it is never typed by hand and a fork's releases carry
          # the fork, not this repo. Untracked like VERSION, so a clone has neither and
          # falls back to its own origin remote.
          echo "${{ github.repository }}" > "${STAGING}/REPO"
          tar czf "${STAGING}.tar.gz" "$STAGING"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: ${{ steps.version.outputs.version }}
          generate_release_notes: true
          files: shai-${{ steps.version.outputs.version }}.tar.gz
