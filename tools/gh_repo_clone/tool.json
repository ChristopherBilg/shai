{
  "name": "gh_repo_clone",
  "description": "Clone a GitHub repository into a temporary directory. Returns the clone path. Optionally checks out a specific branch or tag.",
  "capabilities": {
    "read_only": false
  },
  "input_schema": {
    "type": "object",
    "properties": {
      "repo": { "type": "string", "description": "Repository in OWNER/REPO format." },
      "ref": { "type": "string", "description": "Optional branch or tag to checkout after cloning." }
    },
    "required": ["repo"]
  }
}
