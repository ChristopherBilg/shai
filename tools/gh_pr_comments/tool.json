{
  "name": "gh_pr_comments",
  "description": "List comments on a GitHub pull request (read-only). Returns both review comments (inline on diff) and conversation comments.",
  "capabilities": {
    "read_only": true
  },
  "input_schema": {
    "type": "object",
    "properties": {
      "number": { "type": "string", "description": "The pull request number." },
      "repo": { "type": "string", "description": "Optional OWNER/REPO. Defaults to the repository of the current directory." }
    },
    "required": ["number"]
  }
}
